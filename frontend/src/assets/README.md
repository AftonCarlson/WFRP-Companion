# Frontend Assets

Runtime-public assets live under `frontend/public/assets/` so Vite can serve
them directly. Source assets that are not yet in the frontend pipeline stay in
the repo-level `assets/ui/` directory.
